typedef enum {
    MODE_IDLE,
    MODE_AUTO_SORT,
    MODE_SWEEP,
    MODE_OVERRIDE
} mode_state;

typedef enum {
    SORT_IDLE,     
    SORT_ARD_READY,
    SORT_CAM_READY,
    SORT_IMG_READY
} sort_state;
